{
  "name": "carrier_node_sdk",
  "version": "1.0.0",
  "description": "CarrierNodeSDK",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "gypfile": true,
  "dependencies": {
    "bindings": "^1.5.0"
  }
}
