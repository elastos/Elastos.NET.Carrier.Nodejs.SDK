{
  "name": "ls",
  "version": "1.0.0",
  "description": "```shell     npm install elca ```",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon --config nodemon.json",
    "clean": "rimraf dist",
    "build": "npm run clean && babel src -D --out-dir dist",
    "serve": "npm run build && node dist/app.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "elastos_carrier_addon": "0.0.3",
    "express": "^4.16.3",
    "lodash": "^4.17.5",
    "rimraf": "^2.6.2",
    "socket.io": "^2.1.0"
  },
  "devDependencies": {
    "nodemon": "1.12.1",
    "babel-cli": "^6.24.1",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-stage-2": "^6.24.1",
    "babel-eslint": "^7.2.3",
    "eslint": "^3.19.0"
  }
}
